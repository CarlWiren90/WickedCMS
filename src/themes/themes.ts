const layout = {
  layout: "flex flex-wrap gap-6",
};

const stackContent = {
  vertical: "flex-col",
  horizontal: "flex-row",
};

const cardsTheme = {
  cardsStyling: "flex text-color-black bg-color-white p-6 w-80 rounded-md shadow-md",
  stackContent: stackContent,
  parentContainerLayout: layout,
};

export const themeClasses = {
  cards: cardsTheme,
};
